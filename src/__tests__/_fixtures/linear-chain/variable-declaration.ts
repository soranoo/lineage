const base = 10;
const multiplier = 3;
// oxlint-disable-next-line no-unused-vars This is intentionally unused to test that it is removed by the slicer.
const unrelated = 99;

export const result = base * multiplier;
